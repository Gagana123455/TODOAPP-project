# TODOAPP-project
A To-Do App is a simple web application that allows users to add, view, and delete tasks. It uses HTML to create the structure, CSS to design the interface, and JavaScript to handle user actions like adding and removing tasks dynamically without reloading the page.
